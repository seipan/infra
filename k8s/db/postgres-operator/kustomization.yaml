apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: postgres

helmCharts:
  - name: postgres-operator
    repo: https://cloudnative-pg.github.io/charts
    version: cloudnative-pg-v0.27.0
    releaseName: postgres-operator
    namespace: postgres
    valuesFile: values.yaml
