configs:
  cm:
    url: https://argocd.yadon3141.com
    kustomize.buildOptions: "--enable-helm"
  params:
    controller.diff.server.side: "true"

repoServer:
  enabled: true
  replicas: 2

server:
  extraArgs:
    - --insecure
